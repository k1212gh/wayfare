# ScreenAtlas - App Flow Graph Pipeline
# Core
droidbot>=1.0.0
anthropic>=0.40.0
networkx>=3.0

# Web server
fastapi>=0.115.0
uvicorn>=0.30.0

# Image processing
Pillow>=10.0

# XML / APK parsing
lxml>=5.0
androguard>=4.0

# Utilities
pydantic>=2.0
python-multipart>=0.0.9
aiofiles>=24.0

# UI automation — replaces `adb shell uiautomator dump` CLI which requires
# UI idle state. u2 runs an on-device HTTP agent that returns hierarchy
# instantly even during animations (ticking clocks, ad banners, spinners).
# First `u2.connect(serial)` call triggers auto-install of the agent APK
# on the device; subsequent calls are cheap.
uiautomator2>=3.5.0
